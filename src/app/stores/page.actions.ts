import { createAction } from '@ngrx/store';

export const loadApplication = createAction('[Application Load] Load');

export const loadHomePage = createAction('[Home Page] Load');

export const loadProductsPage = createAction('[Products Page] Load');
