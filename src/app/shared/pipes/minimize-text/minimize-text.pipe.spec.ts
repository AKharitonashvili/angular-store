import { MinimizeTextPipe } from './minimize-text.pipe';

describe('MinimizeTextPipe', () => {
  it('create an instance', () => {
    const pipe = new MinimizeTextPipe();
    expect(pipe).toBeTruthy();
  });
});
