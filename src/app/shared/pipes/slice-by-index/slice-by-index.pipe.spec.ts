import { SliceByIndexPipe } from './slice-by-index.pipe';

describe('SliceByIndexPipe', () => {
  it('create an instance', () => {
    const pipe = new SliceByIndexPipe();
    expect(pipe).toBeTruthy();
  });
});
